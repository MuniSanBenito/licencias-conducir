import type { CollectionConfig } from 'payload'

export const Archivo: CollectionConfig = {
  slug: 'archivo',
  labels: {
    singular: 'Archivo',
    plural: 'Archivos',
  },
  upload: true,
  trash: true,
  fields: [],
}
