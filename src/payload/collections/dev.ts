import type { CollectionConfig } from 'payload'

export const Dev: CollectionConfig = {
  slug: 'dev',
  labels: {
    singular: 'Dev',
    plural: 'Devs',
  },
  upload: true,
  trash: true,
  fields: [],
}
